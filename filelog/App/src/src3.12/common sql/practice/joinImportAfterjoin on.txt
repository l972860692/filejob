    
  select  --distinct uat.priceplangroup uat_priceplangroup
             ordercate,
            uat.orderno uat_orderno, dev.orderno dev_orderno,
            uat.svcno uat_svcno, dev.svcno dev_svcno,
            uat.aitype uat_aitype, dev.aitype  dev_aitype,
            uat.linetype uat_linetype,dev.linetype dev_linetype,
            uat.cibonuscode uat_cibonuscode,dev.cibonuscode dev_cibonuscode,
            uat.equipmodel uat_equipmodel, dev.equipmodel dev_equipmodel,
            uat.ordertype uat_ordertype, dev.ordertype dev_ordertype,
            --uat.priceplangroup uat_priceplangroup,
            uat.rejectcode uat_rejectcode,dev.rejectcode dev_rejectcode
INTO #ALLNO
from
UAT_08_vdr_mt_peg_master
--(select * from  UAT_08_vdr_mt_peg_master where ordercate = 0) 
uat
left join EXT_MT_MP_MASTER dev 
on uat.orderno = dev.orderno 
where
    isnull(UAT.ordercate,'')='0'  
    and UAT.Svceffectivedate >='2014-08-01'
    and UAT.Svceffectivedate <'2014-08-31'
    AND UAT.rejectcode IS NOT NULL

  
    drop table #ALLNO
  
    select * from #ALLNO where uat_orderno='ORH0482001'
  
    select count(uat_orderno) from #ALLNO
    select count(distinct uat_orderno) from #ALLNO
    select count(uat_orderno) from  #ALLNO   group by uat_orderno having(count(uat_orderno) >1)
  
    select * from #ALLNO where uat_orderno='ORH0482001'
    select * from EXT_MT_MP_MASTER where orderno='ORH0482001'
    select * from UAT_08_vdr_mt_peg_master where orderno='ORH0482001'
  