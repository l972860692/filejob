<!DOCTYPE html>
<html>
<body>

<script>
var arr=[1,2,3,4,5,5,8]
//针对每一项进行运算直到直到找到fasle否则返回true
var everyResult=arr.every(function(){
return arguments[0]>5;

})
//针对每一项进行运算知道找到true为止否则返回fasle
var someResult=arr.some(function(){
return arguments[0]>5;

})
//针对每一项进行过滤 返回新数组 不会对原来数组进行改变
var filterResult=arr.filter(function(){
return arguments[0]>3;

})
//针对每一项进行运算 返回新数组 不会对原数组进行改变
var maprResult=arr.map(function(){
return arguments[0]*3;

})





document.write(everyResult)
document.write('</br>')
document.write(someResult)
document.write('</br>')
document.write(filterResult)
document.write('</br>')
document.write(maprResult)
document.write('</br>')
document.write(arr)
document.write('</br>')
arr.map(function(){
if(arguments[0]>6)
{alert(''+arguments[0]+arguments[1]+arguments[2].join(" "));}


})

//forEach 针对每一项进行迭代 
var c=0;
arr.forEach(function(){

  if(arguments[0]>2)
{
   arr[arguments[1]]=c;//我们可以改变数组的值 不同于c#的foreach迭代 
}
})

document.write(arr)
</script>


</body>
</html> 